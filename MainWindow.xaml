<Window x:Name="WindowMain" x:Class="MCPetList.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MCPetList"
        mc:Ignorable="d"
        Title="MCPetList" Height="450" Width="800" Loaded="Window_Loaded"  Style="{StaticResource MCPetListStyle}" Background="#C6C6C6">
    <Grid>
        <ScrollViewer Margin="0,30,0,0">
            <StackPanel x:Name="MainPanel">
            </StackPanel>
        </ScrollViewer>
        <ToolBar Height="30" VerticalAlignment="Top" ToolBarTray.IsLocked="True" Background="#9E9E9E">
            <ToolBar.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="#FCFCFC" />
                    <Setter Property="HorizontalAlignment" Value="Left" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                </Style>
            </ToolBar.Resources>
            <Button x:Name="ButtonSave" ToolTip="Save">
                <Button.Content>
                    <TextBlock Text="💾 Save" Style="{StaticResource MCTextStyle}"/>
                </Button.Content>
            </Button>

            <Button x:Name="ButtonReload" ToolTip="Reload" Click="ButtonReload_Click">
                <Button.Content>
                    <TextBlock Text="🔄 Reload" Style="{StaticResource MCTextStyle}"/>
                </Button.Content>
            </Button>

            <Button x:Name="ButtonAddPlayer" ToolTip="Add Player" Click="ButtonAddPlayer_Click">
                <Button.Content>
                    <TextBlock Text="➕ Add Player" Style="{StaticResource MCTextStyle}"/>
                </Button.Content>
            </Button>

            <Button x:Name="ButtonAddPet" ToolTip="Add Pet" Click="ButtonAddPet_Click">
                <Button.Content>
                    <TextBlock Text="➕ Add Pet" Style="{StaticResource MCTextStyle}"/>
                </Button.Content>
            </Button>
        </ToolBar>

    </Grid>
</Window>
